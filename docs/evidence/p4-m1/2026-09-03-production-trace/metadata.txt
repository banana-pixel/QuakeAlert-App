================================================================================
metadata.txt — P4-M1' PRODUCTION READ-ONLY TRACE, 2026-09-03

RECONSTRUCTED FROM SESSION TRANSCRIPT — ORIGINAL /tmp BUNDLE LOST AFTER TMPFS REBOOT
================================================================================

original file            : /tmp/m1-prod-20260903T125908Z/metadata.txt
                           (226 lines, 12.9K, sealed 2026-09-04T04:25 local)
original bundle path     : /tmp/m1-prod-20260903T125908Z
original still exists    : NO. That directory and every file in it are GONE.
                           /tmp on the workstation is tmpfs; a reboot at
                           2026-09-04T17:32:02 local destroyed the whole bundle.
                           See RECONSTRUCTION_NOTE.txt.
durable location         : docs/evidence/p4-m1/2026-09-03-production-trace/
                           (this directory, inside the repository, committed)

WHAT IS EXACT AND WHAT IS RECONSTRUCTED
  EXACT TRANSCRIPT-PRESERVED — every line from "# metadata.txt" onward below is
  the original text, regenerated by replaying the same shell writer commands
  recorded verbatim in the session transcript (records 1317, 1319, 1321, 1323,
  1344, 1348, 1377) with the same literal values. Replayed with no network and
  no database access. The replay produced 226 lines / 13168 bytes before the two
  annotations named next — the line count and size recorded for the original.
  RECONSTRUCTED METADATA — three things are annotations, not original text:
    1. this header block;
    2. `bundle_path`, which the original expanded from a live variable; the
       original literal value is restored and labelled;
    3. `utc_finish_pass`, which the original expanded from `date -u` at seal
       time. That reading is NOT in the transcript, so it is marked NOT
       RECOVERED and bounded by the surrounding record timestamps. It was not
       invented, guessed, or back-computed.

NOT CLAIMED
  Byte-identity with the lost original. That cannot be established once the
  original files are gone. What is claimed is: same writer commands, same
  literal values, same recorded length.

NOT A NEW MEASUREMENT
  No production validation was rerun to produce this file. No SSH session, no
  database connection, no HTTP request, no deployment. The measured values are
  the 2026-09-03 production run's values, unchanged.

================================================================================
BEGIN REPRODUCED ORIGINAL metadata.txt
================================================================================
# metadata.txt — P4-M1' PRODUCTION READ-ONLY TRACE
schema        : none invented; temporary bundle, not a repo artifact
bundle_path   : /tmp/m1-prod-20260903T125908Z   (ORIGINAL VALUE, restored;
                the replay that regenerated this text ran elsewhere)
bundle_scope  : TEMPORARY. Outside the repository tree. Not gitignored, not committed.
convention    : project's only evidence convention is .sim-evidence/ (P4-M5', gitignored,
                schema'd by server/scripts/sim_evidence.sh). Different milestone —
                NEITHER reused NOR modified.

outcome       : PRODUCTION MEASUREMENT OBTAINED. Trace ran against the real
                production database in a server-enforced read-only session.

utc_start_pass       : 2026-09-03T12:59:08Z
utc_trace_run        : 2026-09-03T20:24:00Z   (VPS clock; stdout mtime 20:24:00.955Z)
utc_after_snapshot   : 2026-09-03T21:05Z
utc_finish_pass      : NOT RECOVERED — the original held a `date -u` reading taken
                       between 2026-09-03T21:17:45Z and 2026-09-03T21:18:00Z
                       (bounds from the session transcript). Not invented here.
clock_agreement      : VPS and workstation UTC clocks agree to the second
last_n               : 200 requested / 51 rows exist in ledger / 51 read

git_head_workstation : 0dd728be0711d132c6e3a17d7aae11d412f8a64e
git_branch           : development
git_dirty            : true   (INTENTIONAL — tree NOT cleaned)
git_status_porcelain :
   M ROADMAP.md
   M android/app/src/main/java/id/web/quakealert/service/WarningNotifier.kt
   M docs/CURRENT_STATE.md
   M docs/TEMP_ANDROID_PHASE4_CHECKLIST.md
   M server/internal/store/event_lifecycle.go
  ?? .hermes/
  ?? server/internal/event/trace.go
  ?? server/internal/event/trace_realsensor_test.go
  ?? server/internal/event/trace_test.go
  ?? server/internal/store/trace_read_test.go
  ?? server/scripts/trace_triggers.go

PRODUCTION ENVIRONMENT IDENTIFIER
  api_host        : api.quakealert.web.id  (remote; IP not bound to any local interface)
  ssh_target      : opc@<prod-ip>  (owner-authorized key; key contents never read)
  vps_hostname    : quakealert-server
  vps_kernel      : Linux 6.12.0-204.92.4.4.3.el9uek.aarch64  (aarch64, Oracle Linux UEK)
  vps_docker      : Docker version 29.7.2
  compose_project : deploy   (/opt/quakealert/deploy/docker-compose.prod.yml)
  db_container    : quakealert-postgis
  db_image        : imresamu/postgis@sha256:817959c176e1ceaab0da093fffb2be2152ca46bd1c3ab20d1b0cc67df57f7b5c
  db_started      : 2026-08-25T02:22:13Z   (up 9 days at trace time)
  db_schema       : schema_migrations version=8 dirty=false
  app_container   : quakealert-server, image quakealert-server:prod
  app_started     : 2026-09-01T05:55:00Z
  app_tracker     : EVENT_TRACKER_ENABLED=true
  app_boot_config : correlation_window 8000000000ns (8s), attach_radius_km 50,
                    independence_cell_km 5, min_independent_cells 2,
                    resolve_after 90000000000ns (90s), sweep_interval 5s
  app_fleet_boot  : active_verified_nodes 6, independence_cells 2
  vps_repo_head   : ca8262b6a8ce6ec0605546a2b31becd5720a963e  (DIFFERENT from workstation HEAD)
  vps_repo_dirty  : true — M deploy/docker-compose.prod.yml,
                    ?? deploy/.env.prod.bak-20260825-102134,
                    ?? server/cmd/canary_main.go, ?? server/internal/decrypttmp.go

COMMAND EXECUTED
  source     : server/scripts/trace_triggers.go  (//go:build ignore, package main)
  reason for : the VPS has NO Go toolchain (command -v go -> not found), so the file
  a binary     could not be 'go run' there. It was cross-compiled on the workstation
               from the working tree at HEAD 0dd728b and uploaded.
  build      : GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -trimpath \
                 -o qa_trace_arm64 scripts/trace_triggers.go   (exit 0)
  sha256     : b154efe7e478fcd275c3129bb39bcec747cc722eb59ec80ffbc0101224a8b24f
               VERIFIED IDENTICAL on the VPS after upload.
  invocation : LAST_N=200 TRACKER_STATS_FILE=/tmp/qa_stats.json \
                 DATABASE_URL=<read-only prod DSN, never echoed> /tmp/qa_trace_arm64
  exit code  : 0
  stdout     : 11515 bytes, 158 lines — captured VERBATIM, not edited
  stderr     : 0 bytes

TRACKER STATS SOURCE
  endpoint   : GET https://api.quakealert.web.id/api/v1/admin/tracker/stats -> 200
  auth       : X-Admin-Key read from /opt/quakealert/deploy/.env.prod into a shell
               variable on the VPS, length-checked only, unset immediately after use.
               The key value was NEVER printed, logged, or copied off the VPS.
  body       : 831 bytes, integer counters + latency histograms only. Contains no
               credential (see internal/event/counters.go:110-146). Archived as
               tracker_stats_prod.json — safe to keep.
  fetched_at : 2026-09-03T15:38:04Z (VPS), i.e. BEFORE the trace run at 20:24:00Z
  re-fetched : 2026-09-03T21:16:01Z — all counters byte-identical (see report)

READ-ONLY STATEMENT
  The production trace completed WITHOUT ANY WRITE STATEMENT.

  Server-enforced, not merely trusted: the DSN handed to the tool carried
    options=-c default_transaction_read_only%3Don
  and the same DSN form was probed immediately before the run:
    show transaction_read_only -> on
  PostgreSQL would have REJECTED any INSERT/UPDATE/DELETE/DDL from that session.
  pgx v5.5.5 passes 'options' through as a RuntimeParam (pgconn/config.go:292-325
  notRuntimeParams list excludes it), so the setting reached the server.

  NO CONTROL WRITE was attempted against production — the spec forbids writing
  merely to prove writes are blocked, and the session setting is proof enough.

  Statements issued against production during this pass, all SELECT/SHOW:
    - row counts on 5 tables, schema_migrations version
    - pg_stat_user_tables n_tup_ins/upd/del/n_live_tup
    - information_schema.columns, \d alert_emissions
    - algo_ver / alert_type / status / event_state / from_state->to_state groupings
    - newest-3 sensor_observations
    - the tool's own three SELECTs (ListLastNObservations, ListStateLogForReplay,
      ListEmissionsForTrace)
  Plus one GET to the admin tracker/stats endpoint (a read endpoint).
  No POST/PUT/PATCH/DELETE to any production API. Tracker never instantiated.

  WRITE-ABSENCE EVIDENCE (pg_stat_user_tables, before -> after the trace):
    alert_emissions      ins=50 upd=0 del=0 live=50  ->  ins=50 upd=0 del=0 live=50
    earthquake_events    ins=31 upd=31 del=0 live=31  ->  ins=31 upd=31 del=0 live=31
    event_state_log      ins=50 upd=0 del=0 live=50  ->  ins=50 upd=0 del=0 live=50
    sensor_observations  ins=49 upd=0 del=0 live=49  ->  ins=51 upd=0 del=0 live=51
    iot_nodes            ins=13 upd=9629 del=3 live=10 -> ins=13 upd=10083 del=3 live=10

STATIC READ-ONLY VERIFICATION OF THE TOOL (before execution, requirement 3)
  Source: server/scripts/trace_triggers.go (338 lines, //go:build ignore).
  Checked on the workstation checkout before cross-compiling:
    - grep -nE 'INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE|CREATE' -> only line 35,
      inside the header comment, ASSERTING their absence. No SQL write verb.
    - grep -n 'NewTracker|Tracker{' -> none. Tracker never instantiated (req 7).
    - grep -n 'net/http' -> none. No HTTP client at all, so no write API can be
      called and no admin key can enter the process (source lines 123-125).
    - grep -n 'ADMIN_API_KEY|X-Admin-Key' -> none.
    - grep -n 'ledger\.|dispatch\.' -> none. No emission path linked in.
  Its only DB calls are three reads, via store:
    ListLastNObservations, ListStateLogForReplay, ListEmissionsForTrace.
  Counters arrive from a FILE (TRACKER_STATS_FILE), not from HTTP.

CREDENTIAL HANDLING
  No password, DSN, API key, private key or secret appears in ANY file of this
  bundle, in any console output, or in any log kept.
  - POSTGRES_PASSWORD: read from /opt/quakealert/deploy/.env.prod into a shell
    variable INSIDE the VPS, interpolated into the DSN, then unset. Never
    printed, never echoed, never copied off the host.
  - The DSN itself was never echoed: it was passed as DATABASE_URL to the tool.
  - ADMIN_API_KEY: read on the VPS, LENGTH ONLY reported (64 chars), unset
    immediately after the single GET. Value never printed.
  - SSH private key /home/vitowiratara/Downloads/ssh-key-2026-08-23.key:
    contents NEVER read, opened, copied, or printed. Only stat'd for mode (600).
  - No search for other credentials or SSH identities was performed (req: 'Do
    NOT try to discover other credentials or SSH identities').
  - The tracker stats body was inspected against internal/event/counters.go:110-146
    (TrackerStats, 23 counters + 3 latency structs): it carries NO credential
    field, so archiving it verbatim is safe.

TOOL-CALL DENIALS DURING THIS PASS (disclosed, not hidden)
  Several commands were blocked by the local permission classifier. Each is
  recorded because a validation record that hides its own gaps is worthless.
  - Denied, NOT retried (would have been credential discovery, forbidden):
      grep '^Host' ~/.ssh/config   ssh-add -l   ssh -v identity diagnostics
  - Denied as transient classifier errors, later succeeded verbatim or via a
    restructured form (heredoc script on the VPS instead of a long inline
    compound command; information_schema.columns instead of psql \d):
      the first authorized ssh, qa_m1_run.sh (x2), several docker exec psql
      one-liners, three scp fetches, one \d sensor_observations.
  - Consequence for the evidence: production_algover_and_emissions.txt could
    not be scp'd; its content was read on the VPS and is transcribed into this
    metadata file instead. Nothing measured was lost.

ALGO_VER AND EMISSION DISTRIBUTION, read from production (transcribed)
  event_state_log.algo_ver     : phase3-1.1/ic=5  x50
  alert_emissions.algo_ver     : phase1-1.0       x50
  earthquake_events.algo_ver   : phase3-1.1/ic=5  x25  and  <NULL>  x6
  The three axes DIVERGE and that is not a defect: alert_emissions.algo_ver is
  stamped by internal/ledger/writer.go:42 with the LEDGER schema version, not
  the tracker's. ListEmissionsForTrace joins on event_id + event_revision, so
  the divergence cannot break the link. The 6 NULL rows are pre-Phase-3 events.
  alert_emissions breakdown (alert_type|severity|event_state|ws_clients):
    EARTHQUAKE_ADVISORY|ADVISORY|UNCONFIRMED|ws=0  x14
    EARTHQUAKE_ADVISORY|ADVISORY|UNCONFIRMED|ws=1  x11
    EVENT_RESOLVED|CONFIRMED|RESOLVED|ws=0         x14
    EVENT_RESOLVED|CONFIRMED|RESOLVED|ws=1         x11
  event_state_log transitions: DETECTED->UNCONFIRMED x25, UNCONFIRMED->RESOLVED x25

TEMPORARY MECHANISM REMOVAL (requirement 10)
  Removed from the production VPS after validation:
    /tmp/qa_trace_arm64        (the cross-compiled read-only tool)
    /tmp/qa_m1_run.sh /tmp/qa_m1_after.sh /tmp/qa_m1_algover.sh
    /tmp/qa_stats.json /tmp/qa_stats2.json
    /tmp/qa_trace_stdout.txt /tmp/qa_trace_stderr.txt
    /tmp/qa_after.txt /tmp/qa_algover.txt
  Verified gone: no file from this pass remains under /tmp on the VPS.
  NO PERMANENT MECHANISM WAS CREATED. Confirmed after cleanup:
    select count(*) from pg_roles where rolcanlogin  ->  1
  i.e. still the single pre-existing login role. No read-only role was added
  (requirement 9 forbids creating one), and nothing needed dropping.
  Read-only access came from a SESSION setting only, which vanished with the
  connection. All 5 production containers remain up.

  PRE-EXISTING, NOT FROM THIS PASS, left untouched: eight /tmp/qa_* scratch
  files dated 2026-08-30 (qa_db.sh, qa_db2.sh, qa_db3.sh, qa_env_check.sh,
  qa_q.sql, qa_out.txt, qa_err.txt, qa_drill.sh) from the earlier
  owner-granted VPS inspection. Two of them (qa_env_check.sh, qa_drill.sh)
  match a credential-bearing pattern. FLAGGED FOR THE OWNER — not deleted,
  because deleting files this pass did not create is outside its scope.

  Workstation scratch removed: /tmp/qa_trace_arm64, /tmp/qa_stats2.json,
  /tmp/m1_run_console.txt, /tmp/m1_trace_start, /tmp/m1_trace_end,
  /tmp/m1_utc_start.

REPO TREE RE-VERIFIED AFTER THE PASS (unchanged, requirement 12)
  git rev-parse HEAD  -> 0dd728be0711d132c6e3a17d7aae11d412f8a64e
  git status --porcelain -> same 5 modified + 6 untracked entries as at start:
     M ROADMAP.md
     M android/app/src/main/java/id/web/quakealert/service/WarningNotifier.kt
     M docs/CURRENT_STATE.md
     M docs/TEMP_ANDROID_PHASE4_CHECKLIST.md
     M server/internal/store/event_lifecycle.go
    ?? .hermes/
    ?? server/internal/event/trace.go
    ?? server/internal/event/trace_realsensor_test.go
    ?? server/internal/event/trace_test.go
    ?? server/internal/store/trace_read_test.go
    ?? server/scripts/trace_triggers.go
  The dirty tree was NOT cleaned. Nothing was staged, committed, pushed or
  deployed. No governance doc was edited. No migration was run.

SECRET SCAN OF THIS BUNDLE (after assembly)
  grep for password/secret/key/DSN patterns: only the prose sentences that
  state a credential was NOT exposed. No value present.
  grep for the production IP: absent (redacted as <PROD_IP_REDACTED>).
  Only dotted-quad in the bundle is 204.92.4.4, part of the kernel version
  string 6.12.0-204.92.4.4.3.el9uek.aarch64 — not an address.
  Long hex strings present are: two git SHAs, the pinned PostGIS image digest,
  and the tool binary sha256. No secrets.

================================================================================
END REPRODUCED ORIGINAL metadata.txt
================================================================================
